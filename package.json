{
  "name": "hfa",
  "version": "0.2.0",
  "description": "Hedge Fund Analyst — installable equity-research skills for Claude Code, Codex, and other agent runtimes.",
  "type": "module",
  "bin": {
    "hfa": "bin/hfa.js"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "bin",
    "src",
    "skills",
    "plugins",
    "registry.json",
    "VERSION",
    ".env.example"
  ],
  "scripts": {
    "validate": "node scripts/validate.js && node scripts/sync-preamble.js --check",
    "build:registry": "node scripts/build-registry.js",
    "check:registry": "node scripts/build-registry.js --check",
    "sync:preamble": "node scripts/sync-preamble.js",
    "check:preamble": "node scripts/sync-preamble.js --check",
    "test:hfa-core": "bash skills/hfa-core/tests/run.sh",
    "validate:release": "npm run validate && npm run check:registry && npm run test:hfa-core",
    "container:terminal": "id=$(docker ps -q --filter label=devcontainer.local_folder=\"$PWD\" | head -1); if [ -z \"$id\" ]; then echo 'No running devcontainer for this folder — run: devcontainer up --workspace-folder .'; exit 1; fi; docker exec -it -w \"/workspaces/$(basename \"$PWD\")\" \"$id\" bash",
    "e2e:task": "bun test/e2e/run.ts --in-container --task",
    "container:copy": "bash test/e2e/container-copy.sh"
  },
  "keywords": [
    "claude",
    "claude-code",
    "codex",
    "skills",
    "agent",
    "finance",
    "equity-research",
    "hedge-fund"
  ],
  "author": "Mohit Kumar",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MohitKumar1991/hedge-fund-analyst.git"
  }
}
