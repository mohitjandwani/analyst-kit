<!-- analyst-kit:epilogue:start — generated by scripts/sync-preamble.js; edit the template in skills/analyst-kit-core/templates/, never this block -->
## Completion (run last)

PLAN MODE EXCEPTION — ALWAYS RUN: writes only to `~/.analyst-kit/`. Replace `OUTCOME` with one
of `DONE` | `DONE_WITH_CONCERNS` | `ERROR` | `ABORT` | `NEEDS_CONTEXT`.

```bash
_AK="$(cat ~/.analyst-kit/core-path 2>/dev/null)"
[ -x "$_AK/bin/analyst-kit-log" ] && "$_AK/bin/analyst-kit-log" end --skill {{SKILL}} --outcome OUTCOME 2>/dev/null || true
```

If this session surfaced a durable pattern, pitfall, or user preference that would save
5+ minutes next time (not obvious, not a transient error), also log it:

```bash
"$_AK/bin/analyst-kit-learn" add '{"skill":"{{SKILL}}","type":"pitfall|pattern|preference","ticker":"<optional>","insight":"<one line>","confidence":7,"ts":"<iso8601 utc>"}'
```
<!-- analyst-kit:epilogue:end -->
